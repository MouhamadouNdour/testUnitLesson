name: Node CI Test
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        run: cd ..
      - name: Install 🔧
        run: npm install
      - name: Test 🚨
        run: npm test
